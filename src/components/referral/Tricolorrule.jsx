// Static decorative divider — no animation needed. Assumes `.tricolor-rule`
// is already defined in your global stylesheet, same as the source markup.
export default function TricolorRule() {
  return <div className="tricolor-rule" />;
}
